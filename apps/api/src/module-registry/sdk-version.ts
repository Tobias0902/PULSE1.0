// SDK version(s) this installation's Core currently supports. A module
// descriptor targeting anything else is refused at registration time
// (CLAUDE.md Decision #7 §14) rather than silently activated.
export const SUPPORTED_SDK_VERSIONS: readonly string[] = ["1"];
